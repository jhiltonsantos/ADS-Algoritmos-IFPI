# entrada
base = float(input('Qual a medida da base do triangulo? '))
altura = float(input('Qual a medida da altura do triangulo? '))

# processamento
area_triangulo = (base*altura) / 2

# saida
print('A area do triangulo sera de: {}'.format(area_triangulo))
